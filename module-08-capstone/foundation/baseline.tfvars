# baseline.tfvars — every pillar weak, on a hardened foundation.
#
# This is the deploy that lets the chain complete. Nothing here is exotic; each
# false is a control you have already built and simply have not switched on.
guardrail_enabled       = false
goal_lock_enabled       = false
tool_least_priv_enabled = false
hitl_approval_enabled   = false
run_audit_enabled       = false
